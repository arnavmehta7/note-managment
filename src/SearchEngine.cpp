#include "SearchEngine.hpp"
#include "Note.hpp"

std::vector<Note*> SearchEngine::search(const Folder& folder, const std::string& query) const {
    // Placeholder for demonstration.
    return std::vector<Note*>();
}